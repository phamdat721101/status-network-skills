{
  "name": "status-network-skills",
  "version": "1.1.0",
  "description": "Status Network — AI Agent Skills & Builder Toolkit",
  "type": "module",
  "bin": {
    "status-network-skills": "bin/install.cjs"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network statusTestnet",
    "install-skills": "node bin/install.cjs",
    "uninstall-skills": "node bin/install.cjs --uninstall"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0"
  }
}
